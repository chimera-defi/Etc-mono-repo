# Archive Notes

`bench/archive/` is for historical benchmark material that is intentionally **non-canonical**.

Current contents include:

- `2026-03-cleanup-pass1/` — Stage 1/2 archived legacy scripts, docs, and generated root files
- `supervisor_runs_202602/` — small archived supervisor-run snapshot kept for history/reference

Rules of thumb:

- Do not add active entrypoints here.
- Prefer moving superseded docs/scripts here in coherent batches.
- If you archive something that used to be canonical, update `bench/HANDOFF.md` and leave a clear pointer from active docs when needed.
