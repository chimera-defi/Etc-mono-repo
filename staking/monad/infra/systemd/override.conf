[Service]
# Example overrides for extra flags, env, or resource limits.
# Use: /etc/systemd/system/monad-validator.service.d/override.conf

# Environment="MONAD_LOG_LEVEL=info"
# ExecStart=
# ExecStart=/usr/local/bin/monad-validator --config /etc/monad/config.toml --rpc
