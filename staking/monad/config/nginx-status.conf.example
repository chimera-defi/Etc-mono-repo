server {
  listen 80;
  server_name status.liquidmonad.xyz;

  location /status {
    proxy_pass http://127.0.0.1:8787/status;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
  }
}
