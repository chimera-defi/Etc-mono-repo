apiVersion: apps/v1
kind: Deployment
metadata:
  name: withdrawal-keeper
  namespace: aztec-staking
  labels:
    app: withdrawal-keeper
spec:
  replicas: 1
  selector:
    matchLabels:
      app: withdrawal-keeper
  template:
    metadata:
      labels:
        app: withdrawal-keeper
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9092"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: withdrawal-keeper
        image: ghcr.io/aztec-staking/withdrawal-keeper:latest
        ports:
        - name: metrics
          containerPort: 9092
        env:
        - name: METRICS_PORT
          value: "9092"
        - name: POLLING_INTERVAL
          valueFrom:
            configMapKeyRef:
              name: aztec-staking-bots-config
              key: WITHDRAWAL_POLLING_INTERVAL
        envFrom:
        - configMapRef:
            name: aztec-staking-bots-config
        - secretRef:
            name: aztec-staking-bots-secrets
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: 9092
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /ready
            port: 9092
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: withdrawal-keeper
  namespace: aztec-staking
  labels:
    app: withdrawal-keeper
spec:
  selector:
    app: withdrawal-keeper
  ports:
  - name: metrics
    port: 9092
    targetPort: 9092
